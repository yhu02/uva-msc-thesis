# Thesis figure manifest

Every data figure below is generated by the committed script
[`chaosprobe/scripts/single_study_figures.py`](../../chaosprobe/scripts/single_study_figures.py)
from the **archived** C1/C2/C3 campaign sessions only — no live cluster access.
Each figure reuses the campaign's confirmatory analysis driver
(`c1_h1_trend`, `c3_h2_dns`, `c2_h3_anova`, `h4_frontier`, `scorecard`), so
every number printed on a figure is the same value the results chapter reports.
The workflow schematic (`fig-01-workflow.png`) is reused from `thesis_figures.py`.

- **Run all commands from `chaosprobe/`** (the data paths below are relative to it).
- Full set: `uv run python scripts/single_study_figures.py --out-dir ../thesis/figures --figure all`
- Subset: append `--figure h1,h5` (choices: `workflow,h1,h2,h3,h4,h5,hotel,design-fix`).
- The `hotel` figure reads the exploratory external-validity campaign via
  `--c1-hotel-dir results/c1-hotel --c2-hotel-dir results/c2-hotel`.
- The ICC bootstrap is seeded (`seed=42`), so regeneration is deterministic for
  identical inputs.

## Source campaigns → archived sessions

Each campaign was frozen at a tagged commit before its analysis; the figure data
is read from the local session directories below (raw archives retained with the
project materials, available on request).

| Campaign | Local session dir (under `chaosprobe/`) | Tests |
|---|---|---|
| C1 dose-response (8 sessions, `pod-delete` churn, `f` sweep) | `results/c1-online-boutique/s01`–`s08` | H1, H4, H5 |
| C2 replication rescue (24 sessions, `node-drain`, `r`×mode) | `results/c2-roundrobin/` | H3 |
| C3 placement + DNS (14 sessions, `pod-delete`, cache off/on) | `results/c3-dns/` | H2 |
| **hotelReservation external validity** (exploratory; 8 C1 + 24 C2 sessions) | `results/c1-hotel/`, `results/c2-hotel/` | H1, H3 (2nd workload) |
| **C4 node-drain dose-response** (exploratory design-fix; 8 sessions) | `results/c4-nodedrain-dose/` | H3/H4/H5 availability axis |

## Figures

One schematic, one figure per hypothesis (H1–H5), and one exploratory
second-workload external-validity figure (`hotel`); wired into the LaTeX by the
`\label{fig:...}` map below.

| Figure | `--figure` | Source | In-text |
|---|---|---|---|
| `fig-01-workflow.png` — ChaosProbe pipeline (mutate → settle → inject → cross-layer collect → provenance-stamp → archive) | `workflow` | none (schematic) | Fig. `fig:workflow` (ch. 3) |
| `fig-h1-dose-response.png` — per-level grand-median east-west p95 vs cross-node fraction `f`; +13.35 % across `f=0→1`, below the 15 % SESOI | `h1` | C1 (`c1_h1_trend`) | Fig. `fig:h1` |
| `fig-h2-conntrack-dns.png` — (a) cache-off conntrack drop packed > spread (reversed, 7/7 pairs); (b) NodeLocal DNSCache removes the drop (packed → ≈0, spread −78 %) | `h2` | C3 (`c3_h2_dns`) | Fig. `fig:h2` |
| `fig-h3-replication-rescue.png` — trough depth (anti-affine 0.0455 < 0.0909 margin) and user-route error (0.632 → 0; clears 0.302 margin) across the three `r`×mode cells | `h3` | C2 (`c2_h3_anova`) | Fig. `fig:h3` |
| `fig-h4-frontier.png` — two-face placement frontier; all 5 C1 placements non-dominated, latency face varies but availability face degenerate | `h4` | C1 (`h4_frontier`) | Fig. `fig:h4` |
| `fig-h5-scorecard-icc.png` — layered-scorecard ICC (mechanism 0.994, availability 0.180, user-tail 0.741) vs naive aggregate 0.066, with 95 % bootstrap CIs and the ICC ≥ 0.5 bar | `h5` | C1 (`scorecard`) | Fig. `fig:h5` |
| `fig-hotel-external-validity.png` — (a) H1 dose-response on hotelReservation (no monotone increase, Page's `L` p=0.99); (b) H3 user-route error rescue (anti-affine r=3 → 0, but below the 0.302 margin). Exploratory, outside the Holm family | `hotel` | C1/C2-hotel (`c1_h1_trend`, `c2_h3_anova`) | Fig. `fig:hotel` |
| `fig-design-fix-availability.png` — (a) availability dose-response under node-drain (trough 1.0 packed → 0.36 spread — the live axis); (b) corrected two-face frontier (availability trough vs east-west p95, a real trade-off). Exploratory design-corrected re-analysis, outside the Holm family | `design-fix` | C4 node-drain (`design_fix_analysis`) | Fig. `fig:design-fix` |
