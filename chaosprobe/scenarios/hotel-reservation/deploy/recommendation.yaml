# DeathStarBench hotelReservation — recommendation (gRPC, port 8085) + mongodb-recommendation
# Source: https://github.com/delimitrou/DeathStarBench/tree/6ecb09706140f8730b5385c08f1386c654c3c526/hotelReservation/kubernetes/reccomend
# Upstream commit: 6ecb0970 (2024-06-27, latest commit touching hotelReservation).
# Adaptations vs upstream: `io.kompose.service` labels -> `app:` (ChaosProbe prober
# convention); images pinned by tag; explicit resources.requests; tcpSocket readiness
# probes added; kompose/istio annotations dropped; mongodb hostPath PV/PVC replaced by
# emptyDir (chaos runs need no persistence; each service re-seeds its database on boot).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation
  labels:
    app: recommendation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendation
  template:
    metadata:
      labels:
        app: recommendation
    spec:
      containers:
        - name: hotel-reserv-recommendation
          image: deathstarbench/hotel-reservation:0.3.5
          command: ["/go/bin/recommendation"]
          ports:
            - containerPort: 8085
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 5
            tcpSocket:
              port: 8085
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 128Mi

---
apiVersion: v1
kind: Service
metadata:
  name: recommendation
  labels:
    app: recommendation
spec:
  type: ClusterIP
  selector:
    app: recommendation
  ports:
    - name: grpc
      port: 8085
      targetPort: 8085

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-recommendation
  labels:
    app: mongodb-recommendation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-recommendation
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mongodb-recommendation
    spec:
      hostname: recommendation-db
      containers:
        - name: hotel-reserv-recommendation-mongo
          image: mongo:4.4.6
          ports:
            - containerPort: 27017
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 5
            tcpSocket:
              port: 27017
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - mountPath: /data/db
              name: data
      volumes:
        - name: data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-recommendation
  labels:
    app: mongodb-recommendation
spec:
  type: ClusterIP
  selector:
    app: mongodb-recommendation
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
