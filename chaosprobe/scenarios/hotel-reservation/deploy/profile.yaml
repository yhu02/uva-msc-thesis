# DeathStarBench hotelReservation — profile (gRPC, port 8081) + memcached-profile + mongodb-profile
# Source: https://github.com/delimitrou/DeathStarBench/tree/6ecb09706140f8730b5385c08f1386c654c3c526/hotelReservation/kubernetes/profile
# Upstream commit: 6ecb0970 (2024-06-27, latest commit touching hotelReservation).
# Adaptations vs upstream: `io.kompose.service` labels -> `app:` (ChaosProbe prober
# convention); images pinned by tag; explicit resources.requests; tcpSocket readiness
# probes added; kompose/istio annotations dropped; mongodb hostPath PV/PVC replaced by
# emptyDir (chaos runs need no persistence; each service re-seeds its database on boot).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile
  labels:
    app: profile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile
  template:
    metadata:
      labels:
        app: profile
    spec:
      containers:
        - name: hotel-reserv-profile
          image: deathstarbench/hotel-reservation:0.3.5
          command: ["./profile"]
          ports:
            - containerPort: 8081
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 5
            tcpSocket:
              port: 8081
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 128Mi

---
apiVersion: v1
kind: Service
metadata:
  name: profile
  labels:
    app: profile
spec:
  type: ClusterIP
  selector:
    app: profile
  ports:
    - name: grpc
      port: 8081
      targetPort: 8081

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memcached-profile
  labels:
    app: memcached-profile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: memcached-profile
  template:
    metadata:
      labels:
        app: memcached-profile
    spec:
      containers:
        - name: hotel-reserv-profile-mmc
          # Upstream env kept for parity; library/memcached ignores MEMCACHED_* vars
          # (it runs with its defaults: 64 MB cache, 4 threads).
          image: memcached:1.6.42-alpine
          env:
            - name: MEMCACHED_CACHE_SIZE
              value: "128"
            - name: MEMCACHED_THREADS
              value: "2"
          ports:
            - containerPort: 11211
          readinessProbe:
            initialDelaySeconds: 5
            periodSeconds: 5
            tcpSocket:
              port: 11211
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi

---
apiVersion: v1
kind: Service
metadata:
  name: memcached-profile
  labels:
    app: memcached-profile
spec:
  type: ClusterIP
  selector:
    app: memcached-profile
  ports:
    - name: memcached
      port: 11211
      targetPort: 11211

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-profile
  labels:
    app: mongodb-profile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-profile
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mongodb-profile
    spec:
      hostname: profile-db
      containers:
        - name: hotel-reserv-profile-mongo
          image: mongo:4.4.6
          ports:
            - containerPort: 27017
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 5
            tcpSocket:
              port: 27017
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - mountPath: /data/db
              name: data
      volumes:
        - name: data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-profile
  labels:
    app: mongodb-profile
spec:
  type: ClusterIP
  selector:
    app: mongodb-profile
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
